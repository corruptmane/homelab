---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zitadel-tls
  namespace: zitadel
spec:
  secretName: zitadel-tls
  dnsNames:
    - auth.corruptmane.xyz
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
